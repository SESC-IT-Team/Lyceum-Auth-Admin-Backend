# Lyceum-Auth-Admin-Backend

Set `ALLOWED_ORIGINS` in `.env` to a JSON array or a comma-separated list of browser origins:

```env
ALLOWED_ORIGINS=["http://localhost:3000","https://admin.example.com"]
# Equivalent: ALLOWED_ORIGINS=http://localhost:3000,https://admin.example.com
```